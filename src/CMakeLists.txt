message("********************************************************")
message("Generating the build pipeline started...") # A message to print.
message("********************************************************")
add_executable(prog main.cpp)
